//
//  Event.swift
//  CountDown
//
//  Created by Nick Gray on 8/9/20.
//
/*
import UIKit

class Event: NSObject {
    var title: String = ""
    var image : UIImage?
    var date : Date?
    
    init(title: String, image: UIImage, date: Date) {
        super.init()
        self.title = title
        self.image = image
        self.date = date
        
    }

    
    /*
    init(dict : NSDictionary) {
        super.init()
        if let title = dict["title"] as? String {
            self.title = title
        }
        if let image = dict["title"] as? UIImage {
            self.image = image
        }
        if let date = dict["date"] as? Date {
            self.date = date
        }
    }
 */
}
 */
